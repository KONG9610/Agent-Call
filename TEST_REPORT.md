# v0.1.1 验证记录

日期：2026-09-22。以下区分模拟测试、真实软件运行和实物话机测试。

| 项目 | 结果 | 证据范围 |
|---|---|---|
| 完成事件/分段 JSON/历史不回放/子代理过滤 | 通过 | Python 回归测试 |
| 去重、范围筛选、开关取消、失败后继续排队 | 通过 | Python 回归测试 |
| 超长任务标题 | 通过 | 限制列表展示长度，防止窗口响应过大 |
| 应用回归测试合计 | **11 项通过** | `python -m unittest test_app -v`，含控制端口独占测试 |
| SIP/RTP/G.711 基线 | **26 项通过** | `python selftest.py`，回环模拟话机 |
| 已打包 PhoneService.exe | **14 项集成检查通过** | 独立数据目录和模拟话机，真实运行发布二进制 |
| 模拟完成事件引发实际 SIP/RTP 通话 | 通过 | 4 通呼叫、4 次 BYE、444 个收到的 RTP 音频包；模拟事件，不冒充真实 Codex 事件 |
| WorkBuddy 新命令入口 | 通过 | 打包二进制 `--call --source workbuddy` 入队并完成模拟通话 |
| 旧 say.py 本机接口 | 通过 | `/notify` 的 source=agent 进入相同队列并完成模拟通话 |
| Edge 网络语音 | 通过 | 打包程序联网合成新中文短句，生成非空 8 kHz WAV |
| 桌面窗口/本机任务列表 | 通过 | 启动打包窗口，加载本机任务列表，程序自行渲染截图后正常退出 |
| 实物 Yealink SIP-T21P E2 | 通过软件链路 | 新程序内置服务注册 1001，真实外呼正常结束，发送 264 个 RTP 音频包 |
| 话机标签和账号显示名称 | 回读确认 | 均已改为 codex，注册名/用户名仍为 1001 |
| 用户实际听到声音 | 待用户确认 | 软件发包成功不能替代听感确认 |
| 真实 Codex 完成事件自动来电 | 待现场确认 | 已核实本机事件格式并启动监听，正在等待另一条真实任务结束验证 |

## 公开包与本机部署的区别

公开发布包不含个人 data 目录，首次启动通知默认关闭。机主本机部署导入了原来的话机参数、文案和音色，并在真机软件链路测试通过后开启通知。

## 尚未验证

其他品牌/固件、独立 Windows 10 机器、远程/云任务、语音输入、WorkBuddy 模型能否每次记得调用。后者属于模型行为，不能通过程序测试保证。

## 0.1.1 界面更新

- WPF 桌面编译和实际窗口启动通过；四个页面均生成预览并检查。
- 隔离配置下验证语速 +25%、音调 +12 Hz、音量 85% 的实时标签、保存及服务端回读。
- 验证恢复默认、音色保存，以及本地语音模式下禁用不支持的滑块。
- 界面验证不拨打真实电话，不覆盖用户现有设置。电话和监听服务沿用前版，以下原有验证范围不因此扩大。
